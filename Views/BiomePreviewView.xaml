<UserControl x:Class="PackEditor.Views.BiomePreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PackEditor.ViewModels"
             xmlns:local="clr-namespace:PackEditor.Models">
    <UserControl.DataContext>
        <vm:PreviewViewModel />
    </UserControl.DataContext>
    <ScrollViewer>
        <ItemsControl ItemsSource="{Binding BiomeCrops}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type local:BiomeCrop}">
                    <Grid>
                        <Image Source="{Binding Crop}" Width="64" Height="64"/>
                        <Image Source="pack://application:,,,/Assets/block_overlay.png" Opacity="0.6" RenderOptions.BitmapScalingMode="NearestNeighbor" Width="64" Height="64" IsHitTestVisible="False" Stretch="Fill" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <Border Background="#80000000" VerticalAlignment="Bottom" Height="20">
                            <TextBlock Text="{Binding Name}" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
